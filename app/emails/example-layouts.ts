/**
 * Slugs marked `isExample` on registry entries (keep in sync with registry).
 * Admin selectability is gated by the `email_example_layouts_dev` feature flag.
 */
export const EXAMPLE_EMAIL_LAYOUT_SLUGS = ["example-kitchen-sink"] as const;
