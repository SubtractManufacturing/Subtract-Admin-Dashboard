# Mesh Conversion Service Configuration (OPTIONAL)
# ================================================
# These environment variables are completely optional.
# The application will work normally without them.
# When not configured, users can still manually upload mesh files.

# Conversion API URL
# Development: http://localhost:8000/api/v1
# Production (Docker): http://conversion-service:8000/api/v1
# Leave empty to disable automatic mesh conversion
CONVERSION_API_URL=

# API timeout in milliseconds (default: 60000 = 60 seconds)
# Increase for large files or slow conversions
CONVERSION_API_TIMEOUT=60000

# Polling interval in milliseconds (default: 2000 = 2 seconds)
# How often to check conversion status for async jobs
CONVERSION_POLLING_INTERVAL=2000

# Example configurations:

# Local development with conversion service running
# CONVERSION_API_URL=http://localhost:8000/api/v1

# Docker Compose setup
# CONVERSION_API_URL=http://conversion-service:8000/api/v1

# Production with dedicated conversion server
# CONVERSION_API_URL=https://conversion.example.com/api/v1
# CONVERSION_API_TIMEOUT=120000
# CONVERSION_POLLING_INTERVAL=3000